{
  "name": "personal-hub",
  "version": "0.1.0",
  "private": true,
  "description": "Personal Operations Hub V0.1 - task lifecycle skeleton (SQLite, zero deps, FakeWorker)",
  "type": "commonjs",
  "main": "src/main.js",
  "scripts": {
    "start": "node src/main.js",
    "test": "node --test \"tests/*.test.js\"",
    "selftest": "node scripts/selftest.js"
  },
  "engines": { "node": ">=22.13" },
  "dependencies": {}
}
